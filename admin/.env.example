PORT=8080
HOST=localhost
LOG_LEVEL=info
APP_KEY=some_random_key
NODE_ENV=development
SESSION_DRIVER=cookie
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_DATABASE=haven
DB_PASSWORD=password
DB_SSL=false
REDIS_HOST=localhost
REDIS_PORT=6379
# Storage path for HAVEN content (ZIM files, maps, etc.)
# On Windows dev, use an absolute path like: C:/haven-storage
# On Linux production, use: /opt/project-haven/storage
HAVEN_STORAGE_PATH=/opt/project-haven/storage